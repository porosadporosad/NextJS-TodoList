export interface TodoType {
    id:string;
    title:string;
    contents:string;
    isDone:boolean;
}

export interface CompanyInfoType {
    name:string;
    desctiption:string;
    image:string;
}

export interface NewTodo {
    title:string;
    contents:string;
}